<UserControl x:Class="Programmation_Systeme_Groupe_B.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Programmation_Systeme_Groupe_B"
        xmlns:vm="clr-namespace:Programmation_Systeme_Groupe_B.ViewModels"
        mc:Ignorable="d"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch">
    <UserControl.Resources>
        <vm:ViewModel x:Key="VM"/>
    </UserControl.Resources>
    <Grid DataContext="{Binding Source={StaticResource VM}}" Height="274" Width="503">
        <Grid.RowDefinitions>
            <RowDefinition Height="75*"/>
            <RowDefinition Height="66*"/>
            <RowDefinition Height="73*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="233*"/>
            <ColumnDefinition Width="228*"/>
            <ColumnDefinition Width="227*"/>
            <ColumnDefinition Width="236*"/>
        </Grid.ColumnDefinitions>
        <Grid.Background>
            <ImageBrush ImageSource="/View/fondMiA.jpg"/>
        </Grid.Background>
        <Button Content="Salut c'est moi" Command="{Binding OpenBrowser}" Visibility="Visible" Click="Button_Click" Grid.Column="2" Grid.Row="1"  Height="37" Margin="0,0,128,93" VerticalAlignment="Bottom" HorizontalAlignment="Right" Width="86" Grid.RowSpan="2" Grid.ColumnSpan="2">
            <UIElement.RenderTransform>
                <MatrixTransform/>
            </UIElement.RenderTransform>
            <Button.Effect>
                <DropShadowEffect/>
            </Button.Effect>
        </Button>
        <Label Content="Label" HorizontalAlignment="Left" Margin="10,52,0,0" VerticalAlignment="Top" Height="61" Width="312" AutomationProperties.Name="labelTest" BorderBrush="Black" Background="White" Grid.ColumnSpan="2" Grid.Column="1"/>
    </Grid>
</UserControl>
